//WAP to find the remainder of a number after dividing it by 2.

#include <stdio.h>
int main()
{
	int n=27;
	int remainder = n%2;
	printf("The remainder of number %d is %d.", n, remainder);
	return 0;
}
